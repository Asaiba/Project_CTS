export default function App() {
  return (
    <main className="app-shell">
      <section className="card">
        <p className="eyebrow">CTS</p>
        <h1>React Frontend Ready</h1>
        <p>
          This project is now set up with React + Vite. Start building your UI in
          <code> src/App.jsx </code> and styles in <code> src/styles.css </code>.
        </p>
      </section>
    </main>
  );
}